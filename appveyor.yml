environment:
  matrix:
    - nodejs_version: "6.6.0"
      ADDON_VERSION: "47"

platform:
  - x64

install:
  - echo %ADDON_VERSION%
  - ps: Install-Product node $env:nodejs_version $env:platform
  - node -v
  - npm install
